/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // @ts-ignore Next.js 16 agent rules disable
  agentRules: false,
  devIndicators: false,
};

export default nextConfig;
